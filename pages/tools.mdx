import Layout from '../components/Layout/Layout';

# Tools Used

For this iteration of my site, I wanted to spend more time planning the tools I'd use to build the optimal site.

Here are some of the most important ones:

## Frameworks

- Next.js

## Styling

- CSS Modules
- Radix UI

## Hosting

- Vercel

## Backend

- React Query

export default ({ children }) => <Layout title="Tools Used">{children}</Layout>;
